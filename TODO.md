# TODO

Subagents:
    1. Add subagents monitoring to HUD - be able to enter a subagent's conversation and see what it's working on 
    2. In general, the subagent loop is very glitchy: 
        - Problems include: model is stuck "thinking" after a subagent is kicked off instead of fully resuming the conversation (and later being restarted by the async thread)
        - No subagent observability


2. Conversation viewing / sorting - No way to sort the conversations. /conversations should show them all, including subagent logs, should show their names, etc.






4. Add tasks
